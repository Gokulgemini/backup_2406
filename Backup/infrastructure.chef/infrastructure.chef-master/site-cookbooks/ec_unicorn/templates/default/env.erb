##### Dropped by Chef changes will be overwritten.  #####
##### Changes need to be added to Chef.             #####
##### Use this to provide environment variables to  #####
##### Unicorn run time.                             #####
#########################################################

<% @environment_vars.each do |key,value|%>
<%= key.to_s.upcase %>=<%= value %>
export <%= key.to_s.upcase %>
<% end %>

# Monit scrubs the environment; which makes Unicorn behave badly sometimes.
export HOME="/home/$USER"
export SHELL="/bin/bash"
