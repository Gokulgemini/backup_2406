defaults: &defaults
  <% if !@server_names.nil? && !@server_names.empty? %>
  servers:
  <% @server_names.each do |server_name| %>
    - <%= server_name %>:11211
  <% end %>
  <% end %>

<%= @rails_env %>:
  <<: *defaults
